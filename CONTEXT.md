# 领域术语表

## 核心概念

### 知识库（Knowledge Base）
**定义**：按业务领域划分的文档集合，用于组织和管理相关的知识内容。

**说明**：
- 采用扁平分类体系，支持动态创建
- 预留多级分类扩展能力（通过parent_id字段）
- 示例：人事制度、财务政策、技术规范、产品文档

### 文档（Document）
**定义**：用户上传的知识文件，支持text、pdf、word、excel等多种格式。

**说明**：
- 每个文档关联到一个知识库
- 包含版本号、状态、文件类型等元信息
- 文件解析后进行分块和向量化

### 文档分块（Document Chunk）
**定义**：文档解析后按语义切分的文本片段，用于向量检索。

**说明**：
- 分块大小可配置（默认300token）
- 保留重叠部分（默认50token）
- 包含元数据（页码、文档ID、版本号等）

### 版本（Version）
**定义**：文档的时间戳版本标识，格式为 `v{major}.{minor}.{timestamp}`。

**说明**：
- 新版本自动生成，无需用户干预
- 检索时优先展示新版本
- 旧版本数据单独标识展示

### 提示词模板（Prompt Template）
**定义**：用于构建RAG查询Prompt的模板，支持变量替换。

**说明**：
- 每个知识库可配置独立模板
- 支持变量：{context}、{question}、{kb_name}
- 无匹配模板时使用全局默认模板

## 业务流程

### 文件上传流程
1. 用户上传文件 → 系统保存文件并解析 → LLM分析内容推断知识库分类 → 展示分类建议 → 用户确认或手动选择 → 分块向量化 → 存储到向量库

### 检索流程
1. 用户提问（可选选择知识库） → 确定目标知识库 → 选择对应提示词模板 → 向量检索 → 重排序 → 按版本优先级排序 → 构建Prompt → 调用LLM生成回答

## 状态定义

### 文档状态
- **UPLOADING**：上传中
- **PROCESSING**：解析处理中
- **COMPLETED**：处理完成
- **FAILED**：处理失败

### 知识库状态
- **ACTIVE**：启用
- **INACTIVE**：禁用

### 模板状态
- **ACTIVE**：启用
- **INACTIVE**：禁用
