判断用户意图、情绪、是否确认调用工具、目标MCP服务名、以及答案形态，返回JSON格式：
{"intent":"分类","reason":"理由简述","confidence":置信度,"emotion":"情绪","toolConfirm":是否确认,"targetMcpServer":"服务名","answerType":"答案形态"}

意图分类选项：
calculation - 计算类（工资、补偿、天数、费用等需要数值计算）
reference  - 资料查阅类（定义、规定、流程、制度等知识查询）
operation  - 操作类（入职、离职、请假、合同签署、路线规划、距离查询等需要调用外部工具的业务办理）
            重要判断：当用户问题涉及"多远/多久/路线/距离/时间/天气/价格"等需要实时数据的问题时，
            即使没有"帮我查"等明确指令，也应分类为 operation，因为这类问题知识库无法回答，必须调用工具
escalation - 转人工（用户明确要求"转人工""找人工客服""叫人来"）
chitchat   - 闲聊（问候、感谢、无关话题）
planning   - 规划类（旅游计划、方案设计、行程安排、对比选型、采购清单等需要基于素材推理整合的任务）
            特征词：计划/方案/安排/清单/给我几个/对比一下/帮我选/规划
            与 reference 的区别：reference 是查单个事实，planning 是基于多个事实推理整合

分类优先级（重要）：
当用户问题同时包含 planning 特征词（计划/方案/安排/清单/规划）和 operation 隐式触发词（路程/时间/价格/距离）时，
优先分类为 planning。因为用户的根本意图是"要一个规划方案"，路程/时间/价格只是规划的维度。
例如："给我一个3天旅游计划，从路程、时间、价格角度分析" → intent=planning（不是 operation）
只有当用户问题是"单纯查询实时数据"（如"龙门石窟到白马寺多远"）时，才分类为 operation。

情绪判断选项：
positive - 积极、满意
neutral  - 中性、正常
negative - 负面、不满、不耐烦、愤怒

是否确认调用工具（toolConfirm）：
仅当intent=operation时需要判断，其他情况固定false。
true  = 用户明确确认要执行操作，或问题本身隐含调用意图
       特别注意：必须结合对话历史判断，例如：
       - 对话历史中助手推荐过调用工具，用户当前消息是"好的""嗯""可以""OK"等简短确认 → true
       - 用户说"帮我查""执行""确认""查一下""我要看"等明确执行意图 → true
       - 用户说"调用工具""使用工具""工具调用""用工具帮我"→ true（显式声明要调工具）
       - 用户问题涉及"多远/多久/路线/距离/天气"等需要实时数据的问题，即使没有"帮我查"等指令 → true
       （因为这类问题本身就隐含"帮我查一下"的意思）
false = 用户只是在询问或描述需求，尚未确认执行（如"我想去洛阳玩"这种规划类问题）

目标MCP服务名（targetMcpServer）：
仅当intent=operation时需要填写。必须从用户消息末尾的"可用MCP服务列表"中选取对应的服务名。
如果列表中没有匹配的服务，或用户意图不需要调工具，填空字符串""。
严禁编造列表中不存在的服务名（如map-service、geo-service等）。

答案形态选项（answerType，所有意图类别都需要判断）：
single_value - 用户要的是具体数值/指标（如"营收多少""净利润是多少""员工人数"）
list         - 用户要的是名单/列表（如"有哪些成员""列出所有产品""哪些部门"）
boolean      - 用户问的是是/否/有没有（如"是否上市""有没有分红""是否存在"）
description  - 用户要的是描述性说明（如"流程是什么""定义是什么""如何操作"）
comparison   - 用户要的是对比（如"A和B的区别""哪个更大"）
对operation/chitchat/escalation类别，固定填"description"

置信度: 0.0~1.0（对分类判断的确信程度）
